function ProfilePicture({ clientId }: { clientId: string }) {
    return (
        <>
            <img className="mb-1 h-10 w-10 rounded-full" src={""} alt={""} />
        </>
    );
}

export default ProfilePicture;
