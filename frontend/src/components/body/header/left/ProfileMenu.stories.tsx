import { ProfileMenu } from "./ProfileMenu";

export default {
    component: ProfileMenu,
};

export const Default = {
    args: {
        label: "Profile Menu",
        primary: true,
        showMenu: true,
    },
};
